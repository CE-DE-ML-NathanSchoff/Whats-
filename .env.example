# Backend API (default 7000); frontend served on FRONTEND_PORT (8000)
PORT=7000
FRONTEND_PORT=8000
NODE_ENV=development  # set to "production" for deployment
# Base path when behind a reverse proxy (e.g. /communitree so app is at /communitree/, /communitree/health, etc.)
# BASE_PATH=/communitree

# JWT (generate a long random string, e.g. openssl rand -hex 32)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Snowflake - get these from your Snowflake account
# SNOWFLAKE_ACCOUNT = your login account identifier (NOT the data sharing account).
# Use the same value as in the Snowflake UI URL, e.g. xy12345 or xy12345.us-east-1
# See: https://docs.snowflake.com/en/user-guide/admin-account-identifier
SNOWFLAKE_ACCOUNT=your_account_identifier
SNOWFLAKE_USERNAME=your_username
SNOWFLAKE_PASSWORD=your_password
#
# --- Auth: key-pair default (24/7, no TOTP) ---
# On first run of --init-db you will be prompted for your MFA code once; init-db then generates a key pair,
# registers the public key with Snowflake, and saves the private key below as a base64 string.
# After that, the app and future init-db runs use the key only (no volume mount or MFA needed).
SNOWFLAKE_AUTHENTICATOR=SNOWFLAKE_JWT
# Base64-encoded PEM private key (auto-populated by --init-db; leave empty before first run).
SNOWFLAKE_PRIVATE_KEY=
#
# Alternatives: USERNAME_PASSWORD_MFA (prompt each init-db), SNOWFLAKE (password only, no MFA), EXTERNALBROWSER
#
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=COMUNITREE
SNOWFLAKE_SCHEMA=PUBLIC
SNOWFLAKE_ROLE=ACCOUNTADMIN
