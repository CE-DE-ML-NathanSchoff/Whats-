# Comunitree Client

React 19 + Vite 7 frontend for Comunitree. All setup, scripts, and API documentation are in the [root README](../README.md).

**Quick start (from repo root):** `npm run dev:client` (with backend running via `npm run dev` in another terminal).
